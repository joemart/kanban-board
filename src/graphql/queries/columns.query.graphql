query GetAllColumns{
    columns {
        board_id,
        id,
        name,
        position  
    }
}

query GetColumn($id: uuid!){
    columns_by_pk(id: $id){
        board_id,
        id,
        name,
        position
    }
}